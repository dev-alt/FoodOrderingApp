<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoodOrdering.MAUI.MainPage">
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Restaurant logo" />

            <Label
                Text="QuickBite"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" 
                HorizontalOptions="Center"/>

            <Label
                Text="Order your favorite dishes&#10;quick and easy"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                HorizontalOptions="Center"/>

            <Button
                x:Name="ViewMenuButton"
                Text="View Menu" 
                SemanticProperties.Hint="Browse our menu and place your order"
                Clicked="OnViewMenuClicked"
                HorizontalOptions="Fill"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"/>

            <Button
                x:Name="ViewOrdersButton"
                Text="My Orders" 
                SemanticProperties.Hint="View your current and past orders"
                Clicked="OnViewOrdersClicked"
                HorizontalOptions="Fill"
                BackgroundColor="{StaticResource Secondary}"
                TextColor="Black"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>